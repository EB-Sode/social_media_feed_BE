services:
  db:
    image: postgres:15
    container_name: sm_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: sm_web
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
      - media_volume:/app/mediafiles
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432


  redis:
    image: redis:7
    container_name: sm_redis
    ports:
      - "6379:6379"


  celery:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: sm_celery
    command: celery -A social_media_feed worker --loglevel=INFO
    volumes:
      - .:/app
    depends_on:
      - redis
      - sm_web
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432

  celery_beat:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: sm_celery_beat
    command: celery -A social_media_feed beat --loglevel=INFO
    volumes:
      - .:/app
    depends_on:
      - redis
      - web
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432

volumes:
  postgres_data:
  media_volume:
